/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['coral-near-warbler-359.mypinata.cloud'], // Add the domain you want to load images from
  },
};

module.exports = nextConfig;
